# Testing environment overrides for PHPUnit.
#
# This file is intentionally minimal and contains no secrets.
# PHPUnit/Laravel will load this file (because APP_ENV=testing) so that
# bootstrap does not attempt to read a missing .env file (which can surface
# as warnings under PHPUnit's error handling).
